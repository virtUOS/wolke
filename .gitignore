# Go
/bin/
*.test
*.out
coverage.*

# Build artifacts: the embedded SPA is generated by `vite build` into internal/web/dist.
# It is produced by the build, not checked in. CI rebuilds it before the embedded binary.
/internal/web/dist/

# Frontend
/web-ui/node_modules/
/web-ui/dist/
/web-ui/.vite/

# Local config & secrets (examples ARE committed; real ones are not)
.env
*.local.yaml

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
